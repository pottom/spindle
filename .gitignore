/spindle
CLAUDE.md
.claude/
docs/brief.md
.DS_Store
.playwright-mcp/
*.png

# ...except the drawings a mark set is cut from, which are the source, not an artefact.
!assets/marks/**/*.png
!assets/signs/*.png

# ...and the logo, which the README shows and the binary carries.
!internal/logo/*.png

# Keys for the services the metadata comes from. They are secrets, and this
# directory is where they land when they are pasted out of a browser.
lastfm.txt
*.key
*secret*
